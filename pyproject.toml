[project]
name = "open-source-vlms"
version = "0.1.0"
description = "A unified OCR platform that supports multiple Vision-Language/OCR models"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio>=6.5.1",
]
