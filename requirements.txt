# Core dependencies
# Note: Using specific versions for DeepSeek-OCR-2 compatibility
torch==2.6.0
torchvision
transformers==4.46.3
tokenizers==0.20.3
accelerate
einops
addict
easydict
sentencepiece

# Gradio - UI framework
gradio>=6.5.1
gradio_client
spaces

# Flash Attention - Optional, will fallback to eager attention if not available
# flash-attn @ https://github.com/Dao-AILab/flash-attention/releases/download/v2.7.3/flash_attn-2.7.3+cu12torch2.6cxx11abiFALSE-cp310-cp310-linux_x86_64.whl

# PDF processing
PyMuPDF

# Image processing
Pillow
opencv-python
matplotlib

# Other utilities
requests
markdown
huggingface_hub
hf_transfer
hf_xet
av
