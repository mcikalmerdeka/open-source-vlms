# Core dependencies
torch==2.6.0
torchvision
sentencepiece

# Git versions required for GLM-OCR and some models
# These are needed for AutoProcessor support
git+https://github.com/huggingface/transformers.git
git+https://github.com/huggingface/accelerate.git
git+https://github.com/huggingface/peft.git

# Other dependencies
accelerate
einops
addict
easydict

# Gradio - UI framework
gradio>=6.5.1
gradio_client
spaces

# Flash Attention - Optional, will fallback to eager attention if not available
# flash-attn @ https://github.com/Dao-AILab/flash-attention/releases/download/v2.7.3/flash_attn-2.7.3+cu12torch2.6cxx11abiFALSE-cp310-cp310-linux_x86_64.whl

# PDF processing
PyMuPDF

# Image processing
Pillow
opencv-python
matplotlib

# Other utilities
requests
markdown
huggingface_hub
hf_transfer
hf_xet
av
